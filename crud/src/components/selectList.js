import React from 'react';

const SelectList = ({ title, url }) => {
    return (
        <div>
            <label>

            </label>
            <select>
                <option value=''>---</option>
            </select>
        </div>
    );
}

export default SelectList;